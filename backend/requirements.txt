# ---- Core web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Realtime ----
python-socketio==5.12.1

# ---- Database / ORM / migrations ----
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# ---- Validation / settings ----
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# ---- Auth / security ----
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0

# ---- Logging ----
structlog==24.4.0

# ---- HTTP client (LLM/Vision REST calls) ----
httpx==0.28.1

# ---- Resilience / rate limiting ----
tenacity==9.0.0
slowapi==0.1.9

# NOTE: Heavy AI/ML deps (groq, google-generativeai, easyocr, faster-whisper,
# Pillow, opencv, numpy) are added in later phases (P3/P4/P5) to keep the
# initial scaffold fast to install and boot.
