// smiles-drawer ships no types; we load the prebuilt UMD bundle dynamically
// (cast to any in the component) to avoid its unbuilt-source entry.
declare module "smiles-drawer";
declare module "smiles-drawer/dist/smiles-drawer.min.js";
